# وصف كامل لواجهة Form Builder لإعادة البناء في Figma

## 📋 جدول المحتويات
1. [نظرة عامة على الواجهة](#نظرة-عامة-على-الواجهة)
2. [الهيكل العام والتبويبات](#الهيكل-العام-والتبويبات)
3. [تبويب بناء الفورم](#تبويب-بناء-الفورم)
4. [تبويب التضمين والصفحات](#تبويب-التضمين-والصفحات)
5. [تبويب إدارة التاقات](#تبويب-إدارة-التاقات)
6. [تبويب إدارة المواقع](#تبويب-إدارة-المواقع)
7. [تجربة المستخدم والحالات](#تجربة-المستخدم-والحالات)
8. [الألوان والتصميم](#الألوان-والتصميم)
9. [الأيقونات المستخدمة](#الأيقونات-المستخدمة)

---

## نظرة عامة على الواجهة

### الوظيفة الأساسية
Form Builder هو نظام لإدارة وبناء النماذج الديناميكية (Forms) في لوحة التحكم الإدارية. يسمح للمسؤولين بإنشاء وتعديل النماذج المستخدمة في الموقع (مثل نموذج المشتري، نموذج البائع) بدون الحاجة لتعديل الكود.

### المكونات الرئيسية
- **إدارة النماذج**: إنشاء، تعديل، وحذف النماذج
- **إدارة الخطوات**: إضافة وتعديل خطوات النموذج مع السحب والإفلات
- **إدارة الحقول**: إضافة وتعديل الحقول داخل كل خطوة
- **المعاينة المباشرة**: معاينة النموذج أثناء البناء
- **إعدادات التضمين**: إدارة كيفية تضمين النموذج في الصفحات

---

## الهيكل العام والتبويبات

### 1. الهيدر الرئيسي (Header)

**الموقع**: أعلى الصفحة، عرض كامل

**العناصر**:
- **العنوان الرئيسي**: 
  - النص: "بناء الفورمات"
  - الحجم: `text-2xl font-bold`
  - المحاذاة: يسار
  
- **الوصف**:
  - النص: "إدارة وتعديل جميع الفورمات"
  - الحجم: `text-sm text-muted-foreground`
  - المسافة: `mt-1`

- **قائمة اختيار النموذج** (Select):
  - العرض: `w-56`
  - النوع: Dropdown Select
  - المحتوى: قائمة بجميع النماذج المتاحة
  - كل عنصر يحتوي على:
    - Badge يوضح نوع النموذج (مشتري/بائع)
    - اسم النموذج (displayName)
  
- **قائمة الإجراءات** (Dropdown Menu):
  - أيقونة: `MoreVertical`
  - الخيارات:
    - تعديل النموذج (مع أيقونة `Edit2`)
    - حذف النموذج (مع أيقونة `Trash2`، لون أحمر)

- **أزرار الإجراءات**:
  - زر "إضافة فورم" (`Plus` + نص)
  - زر "إظهار/إخفاء المعاينة" (`Eye` + نص)

**التخطيط**: Flexbox، العناصر موزعة بين اليسار واليمين

---

### 2. التبويبات الرئيسية (Tabs)

**الموقع**: أسفل الهيدر مباشرة

**التبويبات الأربعة**:
1. **بناء الفورم** (`builder`) - التبويب الافتراضي
2. **التضمين والصفحات** (`embedding`)
3. **إدارة التاقات** (`smart-tags`)
4. **إدارة المواقع** (`locations`)

**التصميم**:
- `TabsList` مع `w-fit`
- كل `TabsTrigger` يحتوي على نص التبويب
- التبويب النشط له خلفية مختلفة

---

## تبويب بناء الفورم

### التخطيط العام

**التقسيم**: 
- **الجانب الأيسر (45% من العرض)**: قائمة الخطوات والحقول
- **الجانب الأيمن (55% من العرض)**: معاينة النموذج (إذا كانت مفعلة)

**الحاوية الرئيسية**:
- `flex gap-6`
- `min-h-0 overflow-hidden` للسماح بالتمرير

---

### القسم الأيسر: قائمة الخطوات

#### الحاوية الرئيسية
- Card component
- `w-[45%]` أو `w-full` إذا كانت المعاينة مخفية
- `flex flex-col min-h-0`
- `ScrollArea` للتمرير العمودي

#### قائمة الخطوات (Accordion)

**الهيكل**:
- `Accordion` مع `type="multiple"` (يمكن فتح أكثر من خطوة)
- كل خطوة هي `AccordionItem`

**عناصر كل خطوة**:

1. **رأس الخطوة** (`AccordionTrigger`):
   - **مؤشر السحب** (`GripVertical`):
     - أيقونة: `GripVertical`
     - الحجم: `w-4 h-4`
     - اللون: `text-muted-foreground`
     - قابل للسحب والإفلات
   
   - **أيقونة الخطوة**:
     - خلفية: دائرة ملونة (`w-8 h-8 rounded-lg`)
     - الألوان المتاحة (دوران):
       - `bg-emerald-100` + `text-emerald-600`
       - `bg-amber-100` + `text-amber-600`
       - `bg-blue-100` + `text-blue-600`
       - `bg-teal-100` + `text-teal-600`
       - `bg-purple-100` + `text-purple-600`
       - `bg-orange-100` + `text-orange-600`
       - `bg-indigo-100` + `text-indigo-600`
       - `bg-pink-100` + `text-pink-600`
     - الأيقونة: من `lucide-react` حسب `step.icon`
   
   - **عنوان الخطوة**:
     - النص: `step.title`
     - الحجم: `font-semibold text-base`
   
   - **Badge عدد الحقول**:
     - النص: عدد الحقول في الخطوة
     - النوع: `variant="secondary"`
     - الحجم: `text-xs`
   
   - **أزرار الإجراءات**:
     - زر "تعديل" (`variant="ghost" size="sm"`)
     - زر "حذف" (أيقونة `Trash2` حمراء)

   **الحالة المحددة**:
   - إذا كانت الخطوة محددة: `bg-primary/10 border-2 border-primary`
   - إذا لم تكن محددة: `bg-muted/30`

2. **محتوى الخطوة** (`AccordionContent`):
   - **قائمة الحقول**:
     - كل حقل في `div` منفصل
     - التصميم: `p-2.5 border rounded-lg hover:bg-muted/30`
     - المحتوى:
       - **اسم الحقل**: `font-medium text-sm`
       - **نوع الحقل**: Badge `variant="outline" text-xs`
       - **Badge إلزامي**: إذا كان `required=true`، Badge أحمر `variant="destructive"`
       - **أزرار**: تعديل وحذف
   
   - **زر إضافة حقل**:
     - `variant="outline" size="sm"`
     - أيقونة `Plus` + نص "إضافة حقل"
     - `w-full h-8`

#### إضافة خطوة جديدة

**الموقع**: أسفل قائمة الخطوات

**التصميم**:
- `border-2 border-dashed rounded-lg p-3`
- **حالة التحرير**:
  - Input لإدخال اسم الخطوة
  - زر "إضافة خطوة" (`Plus` + نص)
  - يتم تفعيل التحرير عند الضغط على Enter أو الزر

**حالة التحرير النشطة**:
- `QuickStepEditor` component
- Input واحد للعنوان
- أزرار: حفظ (`Save`) وإلغاء (`X`)

---

### التحرير السريع للخطوات

#### QuickStepEditor (في Accordion)

**التصميم**:
- `p-3 border-2 border-primary rounded-lg bg-primary/5`
- Input واحد:
  - `placeholder="اسم الخطوة"`
  - `autoFocus`
  - يدعم Enter للحفظ و Escape للإلغاء
- أزرار:
  - حفظ: `Save` icon
  - إلغاء: `X` icon

#### StepEditor (Dialog كامل)

**الحجم**: `max-w-2xl max-h-[90vh] overflow-y-auto`

**الحقول**:
1. **العنوان** (`title`):
   - Label: "العنوان *"
   - Input مع `placeholder="مثال: المعلومات الشخصية"`

2. **الوصف** (`description`):
   - Label: "الوصف"
   - Textarea مع `rows={2}`

3. **الأيقونة** (`icon`):
   - Label: "الأيقونة"
   - Select مع قائمة الأيقونات المتاحة
   - كل خيار يعرض الأيقونة + الاسم

4. **الترتيب** (`order`):
   - Label: "الترتيب"
   - Input نوع `number` مع `min={1}`

5. **إلزامي** (`isRequired`):
   - Switch component
   - Label: "إلزامي"

6. **نشط** (`isActive`):
   - Switch component
   - Label: "نشط"

**الأزرار**:
- حفظ: `Save` + نص "حفظ"
- حذف: `Trash2` (فقط عند التعديل)

---

### التحرير السريع للحقول

#### QuickFieldEditor (في Accordion)

**التصميم**:
- `p-3 border-2 border-primary rounded-lg bg-primary/5 space-y-2`

**الحقول**:
1. **صف أول** (`grid-cols-2 gap-2`):
   - Input اسم الحقل (`name`): `placeholder="اسم الحقل (key)"`
   - Input التسمية (`label`): `placeholder="التسمية"`

2. **صف ثاني** (`flex gap-2`):
   - Select نوع الحقل (`type`)
   - زر تبديل إلزامي/اختياري

3. **إدارة الخيارات** (إذا كان النوع يحتاج خيارات):
   - `OptionsManager` component
   - يظهر فقط بعد حفظ الحقل

**الأزرار**:
- حفظ: `Save` + نص "حفظ"
- إلغاء: `X` + نص "إلغاء"

#### FieldEditor (Dialog كامل)

**الحجم**: Card كامل مع `h-full flex flex-col`

**الحقول**:
1. **اسم الحقل (Key)** (`name`):
   - Input مع `placeholder="propertyType"`
   - التحقق: يجب أن يبدأ بحرف ويمكن أن يحتوي على أرقام وشرطة سفلية فقط

2. **التسمية** (`label`):
   - Input مع `placeholder="نوع العقار"`

3. **النوع** (`type`):
   - Select مع الأنواع المتاحة:
     - نص
     - رقم
     - اختيار واحد
     - اختيار متعدد
     - أزرار (Chips)
     - نطاق
     - اختيار المدينة
     - اختيار الأحياء
     - اختيار نوع العقار
     - الوسوم الذكية
     - خريطة الموقع
     - تاريخ

4. **النص المساعد** (`placeholder`):
   - Input مع `placeholder="اختر نوع العقار..."`

5. **النص التوضيحي** (`helpText`):
   - Textarea مع `rows={2}`

6. **القيمة الافتراضية** (`defaultValue`):
   - Input (يظهر فقط للنوع `text` أو `number`)

7. **إلزامي** (`required`):
   - Switch component

8. **إدارة الخيارات** (`OptionsManager`):
   - يظهر فقط للحقول من نوع `select`, `multi_select`, `chips`
   - Card يحتوي على:
     - Label: "خيارات الحقل (عدد)"
     - قائمة الخيارات (قابلة للتمرير `max-h-64`)
     - كل خيار:
       - عرض: `value` و `label`
       - أزرار تعديل وحذف
     - إضافة خيار جديد:
       - Inputين: القيمة والتسمية
       - زر `Plus`

9. **قواعد العرض الشرطية** (`ConditionBuilder`):
   - Card component
   - الحقول:
     - Select الحقل المرجعي
     - Select العملية (يساوي، لا يساوي، يحتوي على، أكبر من، أصغر من)
     - Input/Select القيمة (حسب نوع الحقل المرجعي)
   - زر لإزالة الشرط (`X`)

10. **إعدادات متقدمة** (`FormFieldConfigEditor`):
    - للحقول الخاصة فقط (city_picker, district_picker, etc.)
    - يظهر فقط بعد حفظ الحقل

**الأزرار**:
- حفظ: `Save` + نص "حفظ"
- حذف: `Trash2` (فقط عند التعديل)

---

### القسم الأيمن: معاينة النموذج

#### الحاوية الرئيسية
- `w-[55%] flex flex-col min-h-0`
- `bg-gray-100/50 rounded-xl p-4`
- `overflow-hidden`

#### الهيدر
- أيقونة `Eye` + نص "المعاينة"
- `text-xl font-bold mb-4`

#### محتوى المعاينة

**FormPreview Component**:

1. **شريط التقدم** (Progress Bar):
   - يظهر فقط إذا كان `currentActiveIndex >= 1`
   - النص:
     - يسار: "بداية موفقة.." أو "اقتربنا من الهدف!" (حسب النسبة)
     - يمين: النسبة المئوية `{reliabilityScore}%` (أخضر `text-green-600`)
   - الشريط:
     - `h-2.5 bg-muted rounded-full`
     - التعبئة: `bg-gradient-to-r from-emerald-500 to-green-400`
     - العرض: حسب النسبة المئوية

2. **حاوية البطاقات المتراكبة**:
   - `relative` مع ارتفاع ديناميكي
   - `HEADER_HEIGHT = 50px`
   - `BASE_CONTENT_HEIGHT = 500px`

3. **البطاقات المكتملة (المطوية)**:
   - تظهر فوق البطاقة النشطة
   - كل بطاقة:
     - `absolute` مع `top: idx * HEADER_HEIGHT`
     - `height: 60px`
     - خلفية ملونة حسب ترتيب الخطوة
     - المحتوى:
       - دائرة ملونة مع أيقونة `Check`
       - عنوان الخطوة (`font-bold text-lg`)
       - زر "تعديل" (`Edit2` + نص)

4. **البطاقة النشطة**:
   - `absolute` مع `top: currentActiveIndex * HEADER_HEIGHT`
   - `bg-white border shadow-xl rounded-2xl`
   - **الهيدر**:
     - `p-5 border-b bg-muted/10`
     - أيقونة الخطوة (دائرة ملونة `w-10 h-10 rounded-xl`)
     - العنوان (`font-bold text-xl`)
     - النص: "الخطوة X من Y"
   - **المحتوى**:
     - `p-6`
     - `DynamicFormRenderer` يعرض حقول الخطوة الحالية فقط
     - زر "التالي" أو "إرسال الطلب":
       - `w-full h-12 text-lg font-semibold rounded-xl shadow-lg`
       - إذا كانت آخر خطوة: `bg-gradient-to-r from-emerald-600 to-green-500`
       - إذا لم تكن: `bg-primary`

---

## تبويب التضمين والصفحات

### FormEmbeddingManager Component

**التخطيط**: `space-y-6`

#### Card: إعدادات التضمين

**CardHeader**:
- `CardTitle`: "إعدادات التضمين"

**CardContent** (`space-y-6`):

1. **الصفحات المرتبطة**:
   - Label: "الصفحات المرتبطة"
   - قائمة Checkboxes:
     - الصفحة الرئيسية (`/`)
     - صفحة إضافة عقار (`/seller-form`)
     - صفحة المستثمر (`/investor`)
   - كل Checkbox مع:
     - Label للنص
     - Badge يوضح المسار (`variant="outline"`)

2. **Shortcode**:
   - Label: "Shortcode"
   - Input مع القيمة الحالية
   - زر نسخ (`Copy` icon)
   - نص توضيحي: "استخدم: [shortcode] في محرر الصفحات"

3. **اسم Component**:
   - Label: "اسم Component"
   - Input
   - نص توضيحي: "اسم Component للاستيراد في الكود"

4. **أمثلة الكود**:
   - **كود Component**:
     - Label + زر نسخ
     - Textarea `readOnly font-mono text-xs h-20`
   - **كود مع Props**:
     - Label + زر نسخ
     - Textarea `readOnly font-mono text-xs h-32`

5. **زر الحفظ**:
   - `w-full`
   - نص: "حفظ الإعدادات" أو "جاري الحفظ..."

---

## تبويب إدارة التاقات

### SmartTagsManager Component

**ملاحظة**: هذا المكون موجود في الكود ولكن التفاصيل الكاملة غير متوفرة في الملفات المفحوصة. يجب أن يحتوي على:
- قائمة التاقات الذكية
- إضافة/تعديل/حذف التاقات
- ربط التاقات بالحقول

---

## تبويب إدارة المواقع

### LocationsManager Component

**ملاحظة**: هذا المكون موجود في الكود ولكن التفاصيل الكاملة غير متوفرة في الملفات المفحوصة. يجب أن يحتوي على:
- إدارة المدن والأحياء
- ربط المواقع بالحقول من نوع `city_picker` و `district_picker`

---

## تجربة المستخدم والحالات

### حالات التحميل

1. **تحميل النماذج**:
   - Spinner في المنتصف
   - `animate-spin rounded-full h-8 w-8 border-b-2 border-primary`

2. **تحميل النموذج المحدد**:
   - Spinner + نص "جاري تحميل النموذج..."
   - `text-center`

### حالات الخطأ

1. **خطأ في التحميل**:
   - أيقونة خطأ أو نص
   - `text-destructive font-semibold`
   - رسالة الخطأ: `text-muted-foreground text-sm`

2. **خطأ في الحفظ**:
   - Toast notification
   - `variant="destructive"`
   - عنوان: "خطأ"
   - الوصف: رسالة الخطأ

### الحالات الفارغة

1. **لا توجد نماذج**:
   - نص: "اختر نموذجاً للبدء"
   - `text-muted-foreground`

2. **لا توجد خطوات**:
   - أيقونة `FileText` كبيرة (`w-12 h-12`)
   - نص: "لا توجد خطوات في هذا النموذج"
   - نص ثانوي: "ابدأ بإضافة خطوة جديدة"

3. **لا توجد حقول**:
   - نص: "لا توجد حقول في هذه الخطوة"
   - نص ثانوي: "اضغط على 'إضافة حقل' لإضافة حقل جديد"

### Toast Notifications

**الأنواع**:
- **نجاح**: خلفية خضراء، أيقونة Check
- **خطأ**: خلفية حمراء، أيقونة X
- **معلومات**: خلفية زرقاء

**المحتوى**:
- عنوان (`title`)
- وصف (`description`)

### Dialogs للتأكيد

1. **حذف النموذج**:
   - `AlertDialog`
   - العنوان: "تأكيد الحذف"
   - الوصف: "هل أنت متأكد من حذف هذا النموذج؟ سيتم حذف جميع الخطوات والحقول المرتبطة به. لا يمكن التراجع عن هذا الإجراء."
   - الأزرار:
     - إلغاء: `AlertDialogCancel`
     - حذف: `AlertDialogAction` مع `bg-destructive`

2. **حذف الخطوة**:
   - نفس التصميم
   - الوصف: "هل أنت متأكد من حذف خطوة '...'؟ سيتم حذف جميع الحقول المرتبطة بها أيضاً."

3. **حذف الحقل**:
   - `confirm()` JavaScript بسيط
   - النص: "حذف هذا الحقل؟"

---

## الألوان والتصميم

### نظام الألوان

**الألوان الأساسية**:
- **Primary**: اللون الأساسي للتطبيق
- **Destructive**: أحمر للحذف والأخطاء
- **Muted**: رمادي فاتح للنصوص الثانوية
- **Background**: أبيض للخلفيات الرئيسية

**ألوان الخطوات** (دوران):
1. `emerald-500` / `emerald-100` / `emerald-600`
2. `amber-500` / `amber-100` / `amber-600`
3. `blue-500` / `blue-100` / `blue-600`
4. `teal-500` / `teal-100` / `teal-600`
5. `purple-500` / `purple-100` / `purple-600`
6. `orange-500` / `orange-100` / `orange-600`
7. `indigo-500` / `indigo-100` / `indigo-600`
8. `pink-500` / `pink-100` / `pink-600`

### المسافات (Spacing)

- **الفجوات الرئيسية**: `gap-6` بين الأقسام الكبيرة
- **الفجوات الصغيرة**: `gap-2` أو `gap-4` بين العناصر
- **Padding للكاردات**: `p-6` أو `p-4`
- **Padding للحقول**: `p-2.5` أو `p-3`

### الحدود والظلال

- **الكاردات**: `border shadow` أو `shadow-xl` للمعاينة
- **الحقول**: `border rounded-lg`
- **الحقول المحددة**: `border-2 border-primary`
- **الحقول المنقطة**: `border-2 border-dashed`

### الخطوط

- **العناوين الرئيسية**: `text-2xl font-bold`
- **العناوين الثانوية**: `text-xl font-semibold` أو `text-lg font-semibold`
- **النصوص العادية**: `text-sm` أو `text-base`
- **النصوص الثانوية**: `text-xs text-muted-foreground`

---

## الأيقونات المستخدمة

### من lucide-react

**الإجراءات**:
- `Plus`: إضافة
- `Trash2`: حذف
- `Edit2` / `Edit`: تعديل
- `Save`: حفظ
- `X`: إلغاء/إغلاق
- `Eye`: معاينة
- `Copy`: نسخ
- `Check`: تأكيد/نجاح
- `MoreVertical`: قائمة إجراءات

**السحب والإفلات**:
- `GripVertical`: مؤشر السحب

**التنقل**:
- `ChevronDown`: فتح
- `ChevronRight`: إغلاق

**النماذج**:
- `FileText`: أيقونة افتراضية للخطوات

**الأيقونات المتاحة للخطوات**:
- User, MapPin, Home, Building2, Ruler, Wallet, CreditCard, Sparkles, Settings, FileText, CheckCircle, Calendar, Clock, Search, Filter, Tag, Star, Heart, ShoppingBag, Briefcase

---

## التفاعلات والحركات

### السحب والإفلات

- **الخطوات**: يمكن سحبها لإعادة الترتيب
- **الحقول**: يمكن سحبها لإعادة الترتيب داخل الخطوة
- **التأثيرات**:
  - أثناء السحب: `opacity: 0.5`
  - المؤشر: `cursor-grab` → `cursor-grabbing`

### الانتقالات

- **البطاقات المتراكبة**: `transition-all duration-500 ease-in-out`
- **شريط التقدم**: `transition-all duration-700`
- **الحقول عند Hover**: `hover:bg-muted/30 transition-colors`

### التحديثات التلقائية

- عند حفظ أي تغيير، يتم تحديث البيانات تلقائياً
- المعاينة تتحدث فوراً عند التعديل
- Toast notifications تظهر تلقائياً عند الإجراءات

---

## ملاحظات مهمة للمصمم

1. **التخطيط المتجاوب**: الواجهة يجب أن تعمل على شاشات مختلفة الأحجام
2. **RTL Support**: جميع العناصر مصممة للغة العربية (RTL)
3. **الوصولية**: يجب التأكد من أن جميع العناصر قابلة للوصول من لوحة المفاتيح
4. **الأداء**: المعاينة يجب أن تكون سلسة ولا تسبب تأخير
5. **الاتساق**: يجب الحفاظ على نفس نمط التصميم المستخدم في باقي لوحة التحكم

---

## الملفات المرجعية

- المكون الرئيسي: `client/src/components/admin/FormBuilder/UltraSimplifiedFormBuilder.tsx`
- قائمة الخطوات: `client/src/components/admin/FormBuilder/StepsList.tsx`
- محرر الخطوة: `client/src/components/admin/FormBuilder/StepEditor.tsx`
- قائمة الحقول: `client/src/components/admin/FormBuilder/FieldsList.tsx`
- محرر الحقل: `client/src/components/admin/FormBuilder/FieldEditor.tsx`
- بناء الشروط: `client/src/components/admin/FormBuilder/ConditionBuilder.tsx`
- المعاينة: `client/src/components/admin/FormBuilder/FormPreview.tsx`
- إدارة التضمين: `client/src/components/admin/FormBuilder/FormEmbeddingManager.tsx`
- إدارة الخيارات: `client/src/components/admin/FormBuilder/OptionsManager.tsx`
- الأنواع: `client/src/components/admin/FormBuilder/types.ts`

---

**تم إعداد هذه الوثيقة بناءً على الكود المصدري الحالي. يُنصح بمراجعة الكود مباشرة لأي تفاصيل إضافية.**

